import { Meta, StoryObj } from '@storybook/react'
import FilterStatusButton from '.'

export default {
  title: 'FilterStatusButton',
  component: FilterStatusButton
} as Meta

export const Default: StoryObj = {}
